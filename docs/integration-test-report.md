# Отчёт о тестировании интеграции модулей ПО MOGGулятор3000

## Дата: 25.09.2026

## Команда: Грузин Егор, Кондрашкин Григорий

---

## Проверенные сценарии интеграции

| №   | Сценарий интеграции                                      | Результат  | Комментарий                                                             |
| --- | -------------------------------------------------------- | ---------- | ----------------------------------------------------------------------- |
| 1   | Нормализация сырого ввода (`MathExpression`)             | ✅ Успешно | Замена пробелов, запятых на точки и `:` на `/` выполнена корректно.     |
| 2   | Передача строки из `MathExpression` в `Evaluator`        | ✅ Успешно | Нормализованная строка без ошибок передана в синтаксический анализатор. |
| 3   | Генерация шагов решения в `StepGenerator` при вычислении | ✅ Успешно | Evaluator автоматически пополняет вектор шагов в `StepGenerator`.       |
| 4   | Сериализация результата в `HistoryEntry`                 | ✅ Успешно | Данные вычисления корректно упакованы в формат JSON.                    |
| 5   | Сохранение записи в `HistoryRepository`                  | ✅ Успешно | Запись успешно добавлена в вектор хранилища и прочитана из него.        |

---

## Обнаруженные ошибки

- Вывод кириллических символов в консоли Windows выводил ошибочную кодировку. Ошибка устранена вызовом `SetConsoleOutputCP(CP_UTF8)`.

## Выводы

Все 5 разработанных C++ модулей успешно интегрированы в единую систему. Взаимодействие между модулями происходит без сбоев.
